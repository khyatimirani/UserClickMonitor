
 -> UserClickMonitor -> UserClickMonitor
    - ERROR | spec: The specification defined in `UserClickMonitor` could not be loaded.

Is a directory @ io_fread - UserClickMonitor

[!] UserClickMonitor did not pass validation, due to 1 error.
You can use the `--no-clean` option to inspect any issue.
